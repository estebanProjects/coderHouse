<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All productos</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <style>
        /* Por alguna razón no puedo ponerlo en un archivo css aparte, he estado intentando y no funciona.
        Así que pido disculpas si no se ve bien que el css esté aquí, estaré investigando por qué no funciona y lo solucionaré. */
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            display: flex;
            flex-direction: column;
            /* margin-top: 150px; */
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #c7516b;
        }

        table {
            margin-top: 30px;
            background-color: white;
            text-align: left;
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
           padding: 20px;
        }

        td img {
            max-width: 85px;
        }

        thead {
           background-color: #243625;
           border-bottom: solid 5px #0f362d;
           color: white;
        }

        tr:nth-child(even) {
            background-color: #ddd;
        }

        tr:hover td {
            background-color: #369681;
            color: white;
        }

        p {
            display: flex;
            margin-top: 25px;
            margin-left: 115px;
            position: absolute;
        }

        p a {
            text-decoration: none;
            color: black;
        }

        p a:hover {
            font-weight: bold;
            color: #0f362d;
            border-bottom: 1px solid black;
        }

    </style>
</head>
<body>

    <h1>Vista de productos</h1>

    <div class="main-container">
        <table>
            <%if (productos.length > 0) { %>
            <thead>
                <tr>
                    <th>Nombre</th><th>Precio</th><th>Foto</th>
                </tr>
            </thead>

                <% productos.forEach(producto => { %>
                    <tr>
                        <td>
                            <%= producto.title %>
                        </td>
                        <td>
                            <%= producto.price %>
                        </td>
                        <td>
                            <img src="
                            <%= producto.thumbnail %>
                            " alt="">
                        </td>
                    </tr>
                <% }) %>    
            <% } else{ %>
                <h2>No se encontraron productos</h2>
            <% } %>

            

    
        </table>

        <p><a href="../">Volver al formulario</a></p>
    </div>

    
</body>
</html>